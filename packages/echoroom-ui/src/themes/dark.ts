/**
 * @echoroom/ui — Dark Theme tokens
 *
 * Dark-mode overrides applied by ThemeProviderUI (via the `.dark` class).
 * Mirrors the `.dark` CSS variables in styles/tokens.css (and web globals.css).
 */
export const darkTheme: Record<string, string> = {
  background: "#0a0a0b",
  foreground: "#fafafa",
  card: "#141416",
  "card-foreground": "#fafafa",
  popover: "#141416",
  "popover-foreground": "#fafafa",
  primary: "#06b6d4",
  "primary-foreground": "#0a0a0b",
  secondary: "#27272a",
  "secondary-foreground": "#fafafa",
  muted: "#18181b",
  "muted-foreground": "#a1a1aa",
  destructive: "#ef4444",
  "destructive-foreground": "#fafafa",
  border: "#27272a",
  input: "#27272a",
  ring: "#06b6d4",
};
